const PORT = 8000;
const CLIENT_PORT = 3000;
const ADDRESS = "10.0.1.3";
const API_PREFIX = "api";
const PROTOCOL = "http";
const DOT = ".";
const MAX_NUM = 9;

const BASE_URL = `${PROTOCOL}://${ADDRESS}:${PORT}/${API_PREFIX}`;

export { PORT, ADDRESS, API_PREFIX, PROTOCOL, BASE_URL, CLIENT_PORT, DOT, MAX_NUM };
